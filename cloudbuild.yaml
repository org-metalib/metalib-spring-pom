steps:
- name: bash
  script: |
    #!/usr/bin/env bash
    echo "=================== Starting"
    apk --no-cache add curl && apk --no-cache add zip
    curl -s "https://get.sdkman.io" | bash
    source "/builder/home/.sdkman/bin/sdkman-init.sh"
    sdk install java 11.0.18-tem
    java -version
    #./mvnw clean
    #sdk version
